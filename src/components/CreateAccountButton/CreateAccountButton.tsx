import React from "react";

const CreateAccountButton = () => {
  return (
    <button
      type="button"
      className="h-10 w-[140px] cursor-pointer rounded-md border border-blue font-medium text-blue"
    >
      Create account
    </button>
  );
};

export default CreateAccountButton;
