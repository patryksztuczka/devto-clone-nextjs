import React from "react";

const CreateAccountButton = () => {
  return (
    <button
      type="button"
      className="h-10 w-[140px] cursor-pointer rounded-md border border-blue font-medium text-blue hover:bg-blue hover:text-white hover:underline"
    >
      Create account
    </button>
  );
};

export default CreateAccountButton;
