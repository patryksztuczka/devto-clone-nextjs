import React from "react";

const CreatePostButton = () => {
  return (
    <button
      type="button"
      className="hover: h-10 w-[110px] cursor-pointer rounded-md border border-blue font-medium text-blue hover:bg-blue hover:text-white hover:underline"
    >
      Create Post
    </button>
  );
};

export default CreatePostButton;
